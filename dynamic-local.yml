http:
  routers:
    # Simple HTTP router for local development
    ctfd-router:
      rule: "Host(`localhost`) || Host(`ctfd.local`)"
      entryPoints:
        - web
      service: ctfd-service

  services:
    ctfd-service:
      loadBalancer:
        servers:
          - url: "http://ctfd:8000"
